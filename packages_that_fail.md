# Windows
```bash
python -m venv venv; venv/Scripts/Activate.ps1; pip install -r requirements.txt; pip install dill; pip install music21; pip install soundfile; pip install pretty_midi
```

# MacOS
```zsh
python3 -m venv venv && source venv/bin/activate && pip3 install -r requirements.txt && pip3 install dill && pip3 install music21 && pip3 install soundfile && pip3 install pretty_midi
```